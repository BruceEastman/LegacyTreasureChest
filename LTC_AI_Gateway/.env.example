# --- AI (Gemini) ---
GEMINI_API_KEY=
# Set to whatever your backend currently expects (example below).
# If your code uses this value directly, keep it in sync with that code.
GEMINI_MODEL=gemini-2.5

# --- Partner Discovery ---
# "stub" (local/dev) or "google" (Google Places API)
PARTNER_DISCOVERY_PROVIDER=stub

# Required only when PARTNER_DISCOVERY_PROVIDER=google
GOOGLE_PLACES_API_KEY=
